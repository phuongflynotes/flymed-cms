import React from "react";
import { Box, List, ListItem, Tooltip } from "@material-ui/core";
import "../../css/searchuser.css";

interface Props {}

const SearchExistingUsers = (props: Props) => {
  return (
    <div className="container">
      <p className="titleContainer">Search for Existing Users to Manage</p>
    </div>
  );
};

export default SearchExistingUsers;
